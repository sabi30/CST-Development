#pragma once

#include "type.h"

namespace cst {

    f32 abs(f32);
    f64 abs(f64);

    f32 sqrt(f32); 
    f64 sqrt(f64);

    f32 clamp(f32, f32, f32);
    f64 clamp(f64, f64, f64);

    f32 max(f32, f32);
    f64 max(f64, f64);
    


}